# Perfil de test: usa H2 en modo PostgreSQL para evitar dependencia externa
spring.datasource.url=jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# JPA con H2
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# RabbitMQ mockeado - no se necesita conexion real para tests de integracion
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# Deshabilitar RabbitMQ listener para evitar conexion
spring.rabbitmq.listener.simple.auto-startup=false

# JWT secret fijo para tests
app.jwt.secret=TestSecretKeyParaIntegracionQueDebeTener32Caracteres!
app.jwt.expiration-ms=86400000
